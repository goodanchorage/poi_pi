GoodAnchorage.com plug-in for OpenCPN

Installation

Windows Installer: http://goodanchorage.com/download/goodanchorage_pi-0.2-win32.exe
Windows DLLs:
   1. Copy 'sqlite3.dll' to OpenCPN folder (C:\Program Files (x86)\OpenCPN):
      http://goodanchorage.com/download/sqlite3.dll
   2. Copy 'goodanchorage_pi.dll' to Plugins folder (C:\Program Files (x86)\OpenCPN\plugins):
      http://goodanchorage.com/download/goodanchorage_pi.dll


Development

git clone git@github.com:goodanchorage/goodanchorage_pi.git

Windows:
cd build
cmake ..   (or Cmake-gui as per http://opencpn.org/ocpn/compiling_source_windows)
cmake --build . --config release
cmake --build . --config debug
cmake --build . --target package --config release
